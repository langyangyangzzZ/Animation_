<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="demo.ht.com.animation">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ButtonAnimatorActivity"></activity>
        <activity android:name=".AnimatorSetActivity" />
        <activity android:name=".PropertyValuesHolderActivity" />
        <activity android:name=".DDActivity" />
        <activity android:name=".ObjectAnimatorActivity" />
        <activity android:name=".PropertyOfObjectActivity" />
        <activity android:name=".PropertyAnimatorActivity" />
        <activity android:name=".TweenAnimation2Activity" />
        <activity android:name=".TweenAnimationActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>